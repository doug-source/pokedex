import { DefaultTheme } from 'styled-components';
import { remValue } from '../../../lib/styling';

export const home: DefaultTheme['home'] = {
    inputFilter: {
        marginTop: `${remValue(66)}rem`,
    },
};
